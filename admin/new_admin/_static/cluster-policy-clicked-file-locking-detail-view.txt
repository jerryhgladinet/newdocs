(Screenshot placeholder: cluster-policy-clicked-file-locking-detail-view.png – This image captures the **File Locking** section of the default group policy.  The breadcrumb bar at the top reads “Tenant Manager > Cluster Dashboard > Default Group Policy > File Locking.”  On this page the administrator can enable file‑locking rules, choose whether to enforce check‑in/check‑out on all files, set an automatic unlock timeout, and decide if users are allowed to force unlock a file.  Numeric fields allow you to specify the lock expiration in minutes and the maximum number of simultaneous locks.)