(Screenshot placeholder: cluster-policy-clicked-filters-detail-view.png – This screenshot shows the **Filters** policy page within the default group policy.  The breadcrumb navigation at the top indicates “Tenant Manager > Cluster Dashboard > Default Group Policy > Filters.”  The page presents two lists: one for **Excluded file extensions** and another for **Included file extensions**.  Administrators can type file extensions (e.g. `.exe`, `.mp3`) to block or allow specific file types when users upload or sync data.  There are also buttons to import or export filter lists.)