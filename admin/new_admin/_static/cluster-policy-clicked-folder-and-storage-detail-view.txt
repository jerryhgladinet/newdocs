(Screenshot placeholder: cluster-policy-clicked-folder-and-storage-detail-view.png – This image depicts the **Folder & Storage** policy configuration under the default group policy.  The breadcrumb bar shows “Tenant Manager > Cluster Dashboard > Default Group Policy > Folder & Storage.”  The page includes settings that define the root path for users’ home folders, set the root location for team folders, and specify storage quotas.  Text fields allow you to configure a path template (e.g. `C:\CentreStackData\{tenant}`) and to enforce per‑tenant storage limits.  There are also checkboxes to automatically create home directories and restrict changes by tenant administrators.)