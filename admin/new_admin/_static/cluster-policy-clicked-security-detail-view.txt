(Screenshot placeholder: cluster-policy-clicked-security-detail-view.png – This image describes the **Security** policy settings under the default group policy.  The breadcrumb bar at the top reads “Tenant Manager > Cluster Dashboard > Default Group Policy > Security.”  On this page administrators can enable or disable multifactor authentication, require re‑authentication when a user’s IP address changes, send email alerts when a user’s email address is updated, and allow cluster administrators to impersonate tenant administrators.  Additional toggles may enforce that all uploads/downloads go through a worker node and restrict external network access for native clients.)